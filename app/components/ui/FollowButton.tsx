import React from 'react';
import { Button } from './button';

const FollowButton = () => {
  return (
    <Button className="ml-2 px-6 py-2 bg-white text-gray-900 hover:bg-gray-200 rounded-full font-semibold">
      Follow
    </Button>
  );
};

export default FollowButton;
